using System;
using System.Numerics;
    class SubsetSums
    {
        static void Main()
        {
            BigInteger S = BigInteger.Parse(Console.ReadLine());
            byte N = byte.Parse(Console.ReadLine());
            for (byte position = 1; position <= N; position++)
            {
                BigInteger number = BigInteger.Parse(Console.ReadLine());
            }
        }
    }
